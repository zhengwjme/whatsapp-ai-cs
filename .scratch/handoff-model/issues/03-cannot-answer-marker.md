# 03: 机器人无法回答：约定标记

**What to build:** 模型答不上来时在回复里附约定标记。机器人去掉标记后，若有正文先按拟人延迟发正文（以机器人身份入历史），再发转人工话术并开启转人工期；只有标记时只发话术。默认 `SYSTEM_PROMPT` 改为指示模型使用该标记；`PROJECT.md` 与 README 中转人工描述同步更新。标记为固定值，不做配置项。

**Blocked by:** 01

**Status:** done

- [x] 模型回复“正文 + 标记”时，客户先收到不含标记的正文，再收到转人工话术，会话处于转人工期
- [x] 模型回复只有标记时，客户只收到转人工话术
- [x] 客户收到的任何消息、会话历史中都不含标记本身
- [x] 不带标记的回复行为与现在一致
- [x] 默认 `SYSTEM_PROMPT` 指示模型在答不上来时输出标记
- [x] 假 LLM 新增带标记模式，e2e 覆盖以上行为
