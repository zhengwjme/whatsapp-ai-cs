# 03: 会话列表显示客户昵称

**What to build:** 管理员在会话列表里除了电话号，还能看到客户的 WhatsApp 昵称，从而认得出是谁。昵称取自客户消息携带的 `pushName`（运营发出的消息不取），每次收到客户消息时更新为最新值，存在新的一张表里（会话 id 为主键）。还没有昵称的会话只显示电话号。

**Blocked by:** 02

**Status:** ready-for-agent

- [x] 归一化单测：客户消息带出 `pushName`，运营消息不带
- [x] 客户改了昵称后，列表显示新昵称
- [x] 会话列表 API 返回昵称；没有昵称时字段为空，页面只显示电话号
- [x] 现有表结构不变，旧数据库启动时能自动建新表
- [x] 现有测试全部通过
