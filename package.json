{
  "name": "whatsapp1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI customer service on a personal WhatsApp number (Baileys), backed by any OpenAI-compatible LLM",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/bot.mjs",
    "test": "node --test",
    "sim": "node --env-file-if-exists=.env scripts/sim.mjs",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "baileys": "7.0.0-rc14",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "patch-package": "^8.0.1"
  }
}
